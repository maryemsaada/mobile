<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme -->
    <style name="Theme.Courses" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here with your own attributes -->
        <item name="colorPrimary">@color/blue_shade_1</item>
        <item name="colorPrimaryDark">@color/blue_shade_2</item>
        <item name="colorAccent">@color/white</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:windowBackground">@color/white</item>

        <!-- Add other style attributes as needed -->
    </style>

    <!-- Customize other components or create additional styles -->
    <style name="CustomTextStyle">
        <item name="android:textColor">#FF0000</item>
        <item name="android:textSize">18sp</item>
    </style>

</resources>
